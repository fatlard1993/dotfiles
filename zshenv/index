#!/bin/zsh

export EDITOR='/usr/local/bin/code'
export VISUAL='/usr/local/bin/code'
export PAGER='less'

zshenvPath="$HOME/.dotfiles/zshenv"

for file in $zshenvPath/*(.); do
	[[ "$file" != "$zshenvPath/index" ]] && source $file
done

for file in $HOME/.zshenv.d/*; source $file