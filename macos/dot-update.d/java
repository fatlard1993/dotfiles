#!/bin/zsh

source "$HOME/.dotfiles/bin/lib/dot-util" "$0" "$@"

# The SVN Java binding symlink this step originally created no longer applies
# on Apple Silicon — Homebrew lives at /opt/homebrew, not /usr/local, and
# libsvnjavahl-1.dylib is not present on this system.
