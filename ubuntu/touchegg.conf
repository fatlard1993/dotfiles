<touchégg>

  <settings>
    <property name="animation_delay">150</property>
    <property name="action_execute_threshold">20</property>
    <property name="color">auto</property>
    <property name="borderColor">auto</property>
  </settings>

  <!--
    3-finger only (touchpad and touchscreen) - 4-finger touch recognition is
    meaningfully less reliable on a touchscreen than 3-finger, and desktop
    switching only needs one gesture pair. Left/right direction matches
    macOS's natural-scroll Spaces convention: swipe left -> next space
    slides in from the right, swipe right -> previous space.

    Desktop switching runs bspc directly rather than sending
    super+period/comma, so the gestures can skip empty desktops
    (.occupied) while the keyboard chords keep stepping through every
    one. The 1-9 and F1-F12 pools always exist whether or not anything
    lives in them (ubuntu/bin/bspwm-desktop-pools), so a plain next/prev
    swipe spends most of its travel on empty desktops. Landing on an
    empty desktop deliberately is still a keyboard move: super+period or
    super+<n>. .local keeps a swipe on the monitor being touched;
    .occupied cycling fails silently when nothing else is occupied,
    which is the wanted no-op.
  -->
  <application name="All">
    <gesture type="SWIPE" fingers="3" direction="LEFT">
      <action type="RUN_COMMAND">
        <repeat>false</repeat>
        <command>bspc desktop -f next.local.occupied</command>
        <on>begin</on>
      </action>
    </gesture>

    <gesture type="SWIPE" fingers="3" direction="RIGHT">
      <action type="RUN_COMMAND">
        <repeat>false</repeat>
        <command>bspc desktop -f prev.local.occupied</command>
        <on>begin</on>
      </action>
    </gesture>

    <gesture type="SWIPE" fingers="3" direction="UP">
      <action type="SEND_KEYS">
        <repeat>false</repeat>
        <modifiers>Super_L</modifiers>
        <keys>Tab</keys>
        <on>begin</on>
      </action>
    </gesture>

    <gesture type="SWIPE" fingers="3" direction="DOWN">
      <action type="SEND_KEYS">
        <repeat>false</repeat>
        <modifiers>Super_L</modifiers>
        <keys>space</keys>
        <on>begin</on>
      </action>
    </gesture>

    <!--
      2-finger pinch, continuous (repeat=true, scales with gesture magnitude
      rather than firing once). ctrl+-/ctrl+= over an actual scroll-wheel
      event since touchegg has no MOUSE_WHEEL action type, and ctrl+-/= is
      the more universally-bound zoom shortcut anyway (browsers, VS Code,
      GIMP, image viewers, terminals).
    -->
    <gesture type="PINCH" fingers="2" direction="IN">
      <action type="SEND_KEYS">
        <repeat>true</repeat>
        <modifiers>Control_L</modifiers>
        <keys>minus</keys>
        <decreaseKeys>equal</decreaseKeys>
      </action>
    </gesture>

    <gesture type="PINCH" fingers="2" direction="OUT">
      <action type="SEND_KEYS">
        <repeat>true</repeat>
        <modifiers>Control_L</modifiers>
        <keys>equal</keys>
        <decreaseKeys>minus</decreaseKeys>
      </action>
    </gesture>
  </application>

</touchégg>
