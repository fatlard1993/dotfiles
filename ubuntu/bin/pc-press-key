#!/bin/bash

sleep 0.4

# forward-key, not bare xdotool - it repairs the stuck-modifier race
# --clearmodifiers has when a physical modifier is released mid-forward.
"$HOME/.dotfiles/ubuntu/bin/forward-key" "$1"