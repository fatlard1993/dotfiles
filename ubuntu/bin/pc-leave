#!/bin/bash

for id in $(bspc query -N -n .window); do
	bspc node "$id" -c
done

sleep 1

[ "$1" == "reboot" ] && sudo reboot
[ "$1" == "shutdown" ] && sudo shutdown -h now
[ "$1" == "exit" ] && bspc quit