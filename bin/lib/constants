#!/bin/zsh

dotfilesPath="$HOME/.dotfiles"
binPath="$dotfilesPath/bin"
libPath="$binPath/lib"
macosPath="$dotfilesPath/macos"
ubuntuPath="$dotfilesPath/ubuntu"

profileName=`cat $HOME/.dotfilesProfile`
profilePath="$dotfilesPath/profiles/$profileName"